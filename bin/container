#!/bin/sh

# If you want download some private packages in any packing platform, you should uncomment that line
# docker cp ~/.ssh/id_rsa $1:/root/.ssh/id_rsa

# Open the bask in container
docker exec -ti $1 /bin/bash